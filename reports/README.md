# Generated reports

The `asteria report` command writes reproducible reports to `reports/generated/`
and figures to `reports/figures/`. Generated artefacts are ignored by Git unless
they are deliberately selected as a stable project example.

